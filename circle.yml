machine:
  services:
    - docker
dependencies:
  override:
    - go get github.com/github/hub
    - go get github.com/github/git-lfs
    - docker info

test:
  override:
    - ./build.sh

deployment:
  production:
    branch: circleci
    commands:
      - ./deploy.sh
